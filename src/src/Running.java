/*
 * Current running process
 */

import java.io.*;
import java.util.*;
import java.lang.*;
import java.text.*;

public class Running
{
    private ReadyList readyList;
    private Clock clock;
    private ArrayList<PCB> current;
    private int startTime;
    private PCB p;
    private PCB firstProcess;




    int p1time = firstProcess.getAT() + firstProcess.getST();

    int followingTime = p1time + p.getST();

    public boolean busy()
    {
        PCB p;
        int timeBusy;
        if (p.getID() == 0);
            timeBusy = p.getAT() + p.getST();
        else
            timeBusy =
        if (p.getAT() )


    }


}
